# claude-ollama-proxy — MTSM Nick

## What this is
A Node.js proxy that translates Anthropic API requests (Claude format) into Ollama/OpenAI format, so you can use Claude Code and other Anthropic tools against a local Ollama model. Also accepts OpenAI-format requests directly, making it a drop-in proxy for OpenAI-compatible clients (Cursor, Continue, LiteLLM, etc.).

## How it works
- Listens on port 4000 by default
- Accepts Anthropic `messages` API format (including tools/tool_use) — `POST /v1/messages`
- Also accepts OpenAI chat format directly — `POST /v1/chat/completions` (no translation, piped through)
- Also accepts legacy OpenAI text completions format — `POST /v1/completions` (converts `prompt` → single user message, returns text_completion envelope; supports streaming)
- Accepts OpenAI embeddings format — `POST /v1/embeddings` (forwarded to Ollama's `/api/embed`)
- Converts Anthropic requests to OpenAI chat format that Ollama understands
- Forwards to Ollama at localhost:11434
- Translates responses back to Anthropic format (OpenAI-format requests are piped verbatim)

## Config

All config is read from environment variables. The proxy also automatically loads a `.env` file from the current working directory (or the script's own directory) at startup — variables already set in the shell environment always take precedence (12-factor style).

```
OLLAMA_MODEL=qwen2.5:7b        (default model)
OLLAMA_HOST=<url>[,<url>...]   (Ollama base URL; comma-separated list for round-robin multi-host; default http://localhost:11434)
PROXY_PORT=4000                (default port)
PROXY_API_KEY=<secret>         (optional; if set, enforces x-api-key / Bearer auth; tracked in metrics under the "default" key name)
PROXY_API_KEYS=<name:key,...>  (optional; comma-separated list of additional named API keys for multi-caller deployments, e.g. "nick:sk-abc,family:sk-def"; combines with PROXY_API_KEY; entries without a "name:" prefix are auto-named key1, key2, ...; lets an operator hand out separate keys per device/user, see per-key usage in /metrics and the dashboard, and revoke a single key without rotating the shared secret)
PROXY_LISTEN_HOST=<host>       (optional; address to bind to; defaults to all interfaces (0.0.0.0); set to 127.0.0.1 to restrict to localhost-only access and prevent LAN/WAN exposure without a firewall)
MODEL_MAP=<json>               (optional; maps claude-* names/prefixes to Ollama models)
PROXY_TLS_CERT=<path>          (optional; path to PEM cert file — enables HTTPS)
PROXY_TLS_KEY=<path>           (optional; path to PEM key file — required when cert is set)
CORS_ORIGIN=<origin>           (optional; Access-Control-Allow-Origin value; default '*')
OLLAMA_NUM_CTX=<n>             (optional; context window size sent to Ollama on every request; model default if unset — often only 2048, set to 32768+ for real sessions)
OLLAMA_KEEP_ALIVE=<duration>   (optional; how long Ollama holds the model in GPU memory between requests, e.g. "30m", "0" to unload immediately, "-1" to keep forever)
PROXY_TIMEOUT=<ms>             (optional; hard per-request timeout in milliseconds; proxy aborts and returns 504 / SSE error if Ollama takes longer; default is no timeout)
PROXY_IDLE_TIMEOUT=<ms>        (optional; idle stream timeout in milliseconds; aborts a streaming response if no new tokens are received from Ollama for this long — useful for detecting a model stalled mid-generation without setting PROXY_TIMEOUT very high; only applies during the active streaming phase; returns request_timeout SSE error; default is no idle timeout)
PROXY_MAX_TOKENS=<n>           (optional; default max_tokens when the client omits it; default 8192)
PROXY_HARD_MAX_TOKENS=<n>     (optional; hard ceiling on output tokens per request — any client-requested max_tokens above this value is silently clamped down; useful on shared deployments to prevent one caller from monopolising the GPU with a huge generation budget; also caps the PROXY_MAX_TOKENS default; applies to POST /v1/messages, /v1/chat/completions, and /v1/completions; default no cap)
PROXY_MAX_BODY_SIZE=<bytes>    (optional; reject requests whose body exceeds this value with 413; Content-Length header is checked immediately (before reading the body), and actual bytes are counted during streaming so clients that omit Content-Length are also enforced; default no limit; example: 10485760 for 10 MB)
LOG_FORMAT=<text|json>         (optional; 'text' emits human-readable lines (default); 'json' emits one JSON object per request for log aggregation tools — Grafana Loki, Datadog, CloudWatch, etc.)
LOG_LEVEL=<info|debug>         (optional; 'info' logs one summary line per request (default); 'debug' also logs the full translated OpenAI-format request body sent to Ollama and, for non-streaming calls, the raw Ollama response body — invaluable for diagnosing why message conversion, system-prompt injection, or tool formatting produces unexpected results; large base64 image payloads are automatically truncated to a `<base64 N chars>` placeholder so logs stay readable)
PROXY_WARMUP=true              (optional; when 'true', sends a minimal preflight request to Ollama after startup to pre-load the model into GPU memory, eliminating cold-start latency on the first real request; default false)
RATE_LIMIT_RPM=<n>            (optional; global request rate limit in requests/minute across all callers; applies to POST /v1/messages, /v1/chat/completions, /v1/completions, /v1/messages/count_tokens, /v1/embeddings, /v1/messages/batches, /v1/models/pull, and DELETE /v1/models/:id; returns 429 with retry-after header; default no limit)
RATE_LIMIT_PER_IP_RPM=<n>     (optional; per-client-IP rate limit in requests/minute; buckets by the raw socket address unless PROXY_TRUST_PROXY=true; default no limit)
RATE_LIMIT_PER_KEY_RPM=<n>    (optional; per-API-key rate limit in requests/minute; buckets by the caller's matched PROXY_API_KEYS name, or "default" when no API keys are configured (or PROXY_API_KEY is used); lets an operator give each caller in a multi-key deployment its own request budget so one device can't exhaust the shared GPU even from behind a NAT shared with other callers; all three rate limits (global, per-IP, per-key) may be active simultaneously; default no limit)
PROXY_TRUST_PROXY=true        (optional; when 'true', RATE_LIMIT_PER_IP_RPM reads the client IP from the x-forwarded-for header instead of the raw socket address; default false — only enable this when the proxy sits behind a reverse proxy/load balancer that overwrites x-forwarded-for with the real client IP, otherwise any direct caller can spoof a fresh IP on every request and bypass the per-IP limit entirely)
PROXY_MAX_CONCURRENCY=<n>     (optional; maximum simultaneous in-flight Ollama inference requests; when the limit is reached and there is no queue (or queue is full) new requests get 503 overloaded_error + Retry-After: 1; applies to POST /v1/messages, /v1/chat/completions, /v1/completions; embeddings and token-counting are not gated; default no limit)
PROXY_MAX_QUEUE_SIZE=<n>      (optional; number of requests that may wait in a queue when all concurrency slots are taken; when the queue is also full the caller still gets 503 immediately; pairs with PROXY_MAX_CONCURRENCY to absorb bursts instead of shedding load; default no queuing — 503 at max concurrency)
PROXY_MAX_QUEUE_TIMEOUT=<ms>  (optional; how long a queued request waits before being abandoned with a 503; only meaningful when PROXY_MAX_QUEUE_SIZE is set; default no timeout — waits until a slot opens)
PROXY_SYSTEM_PROMPT=<text>    (optional; text prepended to every request's system prompt; if the client already sends a system prompt the proxy's text comes first, separated by two newlines; useful for enforcing consistent model behavior across all callers without modifying client config)
PROXY_FORCE_THINK=true         (optional; when 'true', always adds think:true to every outgoing Ollama inference request, enabling native chain-of-thought reasoning for thinking models like DeepSeek-R1 and Qwen3-thinking without requiring the client to send thinking:{type:"enabled"} on each request; safe for non-thinking models — Ollama ignores the parameter when the model doesn't support it; applies to POST /v1/messages, /v1/chat/completions, /v1/completions, and batch processing; default false)
PROXY_AUTO_TRUNCATE=true       (optional; when 'true' and OLLAMA_NUM_CTX is set, automatically drops the oldest user/assistant turns from the message history whenever the estimated input token count would exceed OLLAMA_NUM_CTX, preventing "context length exceeded" 400 errors from Ollama in long conversations; uses chars/4 token estimate; always preserves the system message and at least the last 2 messages; skips orphaned tool/assistant messages at the new head so the remaining history stays valid OpenAI format; sets x-context-truncated response header with the number of messages dropped when truncation occurs; opt-in so existing deployments see no behaviour change; applies to POST /v1/messages, /v1/chat/completions, and batch items; default false)
OLLAMA_OPTIONS=<json>         (optional; JSON object of arbitrary Ollama model parameters applied to every request — repeat_penalty, mirostat, num_gpu, tfs_z, typical_p, etc.; per-request client params take precedence; OLLAMA_NUM_CTX and OLLAMA_KEEP_ALIVE take highest precedence over this; example: {"repeat_penalty":1.1,"mirostat":2,"num_gpu":33})
PROXY_BATCH_PERSIST_PATH=<path> (optional; path to a JSON file where Messages Batch API state (requests + results) is saved after every change and reloaded at startup; without this, batches and their results live only in memory and are lost on restart; batches that hadn't reached 'ended' status when the proxy last stopped are automatically resumed, skipping any items that already have a recorded result; default unset — in-memory only)
```

### MODEL_MAP example
```
MODEL_MAP='{"claude-3-haiku":"qwen2.5:7b","claude-3-sonnet":"qwen2.5:14b","claude-3-opus":"qwen2.5:72b"}'
```
Exact match wins; if no exact match, any key that is a prefix of the requested model name is used (e.g. `"claude-3-haiku"` matches `claude-3-haiku-20240307`). Non-`claude-*` model names always pass through unchanged.

## How to run
```bash
node proxy.js
```
Then point Claude Code at http://localhost:4000 instead of the Anthropic API.

## What's implemented
- `.env` file loading — proxy reads a `.env` file from `process.cwd()` (or `__dirname` if different) at startup; shell environment variables always take precedence; supports `KEY=VALUE`, single/double-quoted values, `#` comments, and blank lines; `parseDotEnv()` helper exported for unit testing
- SSE streaming with full Anthropic event sequence
- tool_use / tool_result round-trip (streaming and non-streaming)
- Image content block support (base64 and URL sources → OpenAI vision format)
- Per-request model selection (pass any non-claude-* model name in the request)
- GET /v1/models — lists models available in Ollama; when MODEL_MAP is configured, also exposes the mapped Claude alias names (e.g. `claude-3-haiku`) so model-picker clients (Cursor, Continue, OpenWebUI) can discover and select them without knowing the underlying Ollama model name; aliases that clash with a real Ollama model ID are suppressed to avoid duplicates
- GET /health — checks Ollama reachability, returns model + port
- Graceful error handling when Ollama is offline (502 with hint)
- Request logging (method, path, status, duration, tokens_in, tokens_out, model) to stdout; LOG_FORMAT=json emits machine-parseable JSON for log aggregation (Grafana Loki, Datadog, CloudWatch, etc.)
- Keepalive SSE comments every 15 s to survive reverse-proxy timeouts
- Graceful shutdown on SIGTERM / SIGINT
- Optional API key auth via PROXY_API_KEY (x-api-key or Authorization: Bearer)
- README.md with full setup instructions
- Dockerfile + docker-compose.yml (Ollama + proxy, health-check gated startup)
- claude-ollama-proxy.service — systemd unit for always-on Linux deployment
- Retry with exponential backoff on transient Ollama 5xx errors (up to 3 retries: 500 ms, 1 s, 2 s)
- Correct streaming output_tokens: message_delta deferred until trailing usage chunk is consumed
- top_k forwarding — passed through to Ollama's OpenAI-compat endpoint
- POST /v1/messages/count_tokens — uses Ollama /api/tokenize for accuracy, falls back to chars/4
- Streaming message_delta includes both input_tokens and output_tokens from trailing Ollama usage chunk
- Streaming message_start.usage.input_tokens — populated with a chars/4 heuristic estimate computed from the serialised OpenAI messages before the stream begins, so SDK clients that read input_tokens from message_start (the Anthropic-specified location) see a meaningful non-zero count rather than 0; the accurate Ollama-reported value is still provided in message_delta.usage.input_tokens for clients that read it there
- Router-level try/catch prevents handler throws from becoming unhandled promise rejections (crash)
- Process-level uncaughtException/unhandledRejection handlers keep server alive on stray async errors
- URL routing strips query params (?foo=bar variants no longer 404)
- Non-streaming path guards against empty Ollama choices array
- MODEL_MAP env var — routes claude-* model names/prefixes to specific Ollama models; resolveModel() handles exact then prefix matching; startup log prints each mapping
- Client abort propagation — AbortController tied to client socket close; cancels in-flight Ollama fetch when caller disconnects (e.g. Ctrl+C in Claude Code), freeing GPU resources immediately; AbortErrors silently discarded; fetchWithRetry never retries them
- TLS / HTTPS support — set PROXY_TLS_CERT + PROXY_TLS_KEY to enable; proxy creates an https.createServer with those PEM files; startup logs TLS status; exits with a clear error if files cannot be read
- Streaming robustness: fallback for streams that end without an explicit finish_reason — defaults stop_reason to end_turn and closes any open content blocks before emitting message_delta/message_stop
- `<think>` tag extraction — both streaming and non-streaming paths detect `<think>…</think>` blocks from thinking models (DeepSeek-R1, Qwen3, etc.) and convert them to Anthropic `thinking` content blocks; streaming uses a state machine with tag-boundary buffering; thinking blocks carry a synthetic signature placeholder; zero-overhead fast path when no `<think>` tag is present; **interleaved thinking fully supported** — multiple alternating `<think>`/text pairs each get their own sequentially-indexed Anthropic content block (compatible with `anthropic-beta: interleaved-thinking-2025-05-14`); the streaming state machine uses a monotonic `nextBlockIdx` counter shared across think/text/tool blocks so indices are always correct regardless of interleaving depth
- Streaming thinking `signature_delta` — emits a `signature_delta` event before each `content_block_stop` for thinking blocks, completing the Anthropic extended-thinking streaming protocol so clients that validate thinking signatures don't reject the response
- Prompt-caching compat fields — all usage objects (streaming `message_start`, `message_delta`, and non-streaming response) include `cache_creation_input_tokens: 0` and `cache_read_input_tokens: 0`; Claude Code sends `anthropic-beta: prompt-caching-2024-07-31` on every request and expects these fields
- `document` content block support — Anthropic `document` blocks in user messages, system prompts, and tool results are converted to text for Ollama; text-source documents pass through directly; base64-encoded text documents are decoded; binary (PDF) and URL sources get an informative placeholder; block title is preserved as a header
- CORS support — all responses include Access-Control-Allow-Origin/Methods/Headers; OPTIONS preflight requests return 204 immediately so browser-based callers work without a separate CORS proxy; CORS_ORIGIN env var restricts the allowed origin (default '*')
- `seed` parameter forwarding — passed through to Ollama for reproducible outputs
- `disable_parallel_tool_use` forwarding — maps Anthropic's disable_parallel_tool_use:true to OpenAI's parallel_tool_calls:false
- `thinking` parameter forwarding — Anthropic's `thinking:{type:"enabled",budget_tokens:N}` maps to Ollama's `think:true` (Ollama 0.7+); supported models (Qwen3-thinking, DeepSeek-R1, etc.) natively emit `<think>` blocks which the proxy's existing state machine converts to Anthropic thinking content blocks
- `OLLAMA_NUM_CTX` env var — sets `num_ctx` on every Ollama request to override the model's default context window (often only 2048 tokens, far too small for Claude Code sessions); set to 32768+ in production
- `OLLAMA_KEEP_ALIVE` env var — sets `keep_alive` on every Ollama request to control how long the model stays loaded in GPU memory between requests; useful for tuning GPU utilisation vs. latency
- `OLLAMA_HOST` env var — overrides the Ollama base URL (default `http://localhost:11434`); accepts a comma-separated list of URLs for round-robin load distribution across multiple Ollama instances/GPUs; `getOllamaHost()` picks the next host in rotation; each request handler captures its host at the start so retries within a single request always hit the same host; GET /health checks all configured hosts in parallel and reports per-host status in a `hosts` array while keeping backward-compat `ollama`/`ollamaError` fields derived from the first host
- Thinking block round-trip in conversation history — `thinking` content blocks in assistant messages are converted back to `<think>…</think>` tags when sending conversation history to Ollama, so multi-turn sessions with thinking models (DeepSeek-R1, Qwen3-thinking) preserve full chain-of-thought context across turns
- GET /metrics — in-memory request metrics endpoint: uptime, per-route request counts, HTTP status code breakdown, p50/p95/p99 latency percentiles (rolling 1000-sample window), cumulative input/output token totals, and current active streaming connection count; no auth required (operational data only)
- `PROXY_TIMEOUT` env var — optional hard per-request timeout (ms); if Ollama does not complete within this window the proxy aborts the in-flight fetch and returns a 504 JSON error (non-streaming) or an SSE error event (streaming); timeout fires via AbortController reusing the existing client-abort signal so GPU resources are released immediately; timedOut flag distinguishes timeout from client disconnect so the response path can send the correct error instead of silently closing; default is no timeout
- `PROXY_IDLE_TIMEOUT` env var — optional idle stream timeout (ms); if no new tokens are received from Ollama for this many milliseconds during an active streaming response, the proxy aborts the stream and sends a `request_timeout` SSE error event; complements `PROXY_TIMEOUT` (which limits total request time) for detecting a model that has stalled mid-generation without needing PROXY_TIMEOUT to be set very high; reuses the same AbortController and timedOut infrastructure as PROXY_TIMEOUT; timer resets on every received chunk so normal slow-but-steady generation is unaffected; applies to POST /v1/messages, /v1/chat/completions, and /v1/completions streaming paths; default is no idle timeout
- `PROXY_MAX_TOKENS` env var — configurable default max_tokens applied when the client omits the field (default 8192); useful for models with larger output budgets or strict token limits
- GET /v1/models/:modelId — looks up a single model by ID from Ollama's model list; also resolves MODEL_MAP aliases (exact and prefix) so `GET /v1/models/claude-3-haiku` returns the aliased model when MODEL_MAP is configured — now consistent with GET /v1/models which already exposed aliases; 404 if the model isn't in Ollama or the alias target isn't; 502 if Ollama is unreachable; colon-separated names (e.g. `qwen2.5:7b`) are URL-decoded automatically
- `version` field in GET /health — proxy package.json version included in every health response and the GET / live dashboard Config card for easier debugging and monitoring
- `OLLAMA_KEEP_ALIVE` exposed in dashboard — the configured keep_alive value now shows in the Config card of the GET / live dashboard alongside num_ctx and other tuning knobs
- Input validation — POST /v1/messages now validates that `messages` is present and is an array, returning a 400 `invalid_request_error` (with a descriptive message) instead of crashing into a 500; `stream` field now correctly defaults to `false` per the Anthropic API spec when not specified by the client
- `request-id` response header — every response carries a unique `req_`-prefixed identifier that matches Anthropic's API header naming; useful for correlating proxy logs with client-side errors
- `PROXY_MAX_BODY_SIZE` env var — optional hard limit on request body size (bytes); enforced at two points: Content-Length header is checked immediately before reading (fast rejection for well-behaved clients), and actual bytes are counted during body streaming so clients that omit Content-Length are also enforced; returns 413 `request_too_large`; protects against runaway base64-image payloads; default is no limit
- GET /metrics/prometheus — Prometheus text exposition format (version 0.0.4) of the same metrics as GET /metrics; exposes `proxy_uptime_seconds`, `proxy_requests_total{method,path}`, `proxy_http_responses_total{code}`, `proxy_request_duration_ms` summary (p50/p95/p99 + _sum + _count), `proxy_tokens_total{direction}`, `proxy_active_streams`, `proxy_errors_total`, and `proxy_request_latency_{min,max,avg}_ms` gauges matching the JSON /metrics fields; scraped directly by Prometheus without any exporter; Content-Type `text/plain; version=0.0.4; charset=utf-8`
- `PROXY_WARMUP` env var — when set to `true`, fires a minimal preflight request to Ollama immediately after the server starts listening; this pre-loads the configured model into GPU memory so the first real Claude Code request incurs no cold-start latency; uses a 3-minute timeout (large models can take time on first load); warmup failures are logged as warnings and do not prevent the proxy from serving traffic
- Retry jitter — exponential backoff delays now include ±25% random jitter to prevent thundering herd when multiple concurrent requests all retry at the same moment (especially relevant in multi-host round-robin deployments)
- Extended latency metrics — GET /metrics now includes `latency_min_ms`, `latency_max_ms`, and `latency_avg_ms` alongside the existing p50/p95/p99 percentiles for a more complete picture of request latency distribution
- Per-model usage metrics — GET /metrics includes `models_usage` object breaking down request counts, input tokens, and output tokens by Ollama model name; GET /metrics/prometheus exposes `proxy_model_requests_total{model}` and `proxy_model_tokens_total{model,direction}` counters; especially useful with MODEL_MAP deployments to compare usage across models
- Rate limiting — `RATE_LIMIT_RPM` (global), `RATE_LIMIT_PER_IP_RPM` (per caller IP), and `RATE_LIMIT_PER_KEY_RPM` (per API key) fixed-window request caps applied to POST /v1/messages, /v1/chat/completions, /v1/completions, /v1/messages/count_tokens, and /v1/embeddings; exceeded limit returns 429 `rate_limit_error` with `retry-after` header; every rate-limited response also carries `x-ratelimit-limit-requests`, `x-ratelimit-remaining-requests`, and `x-ratelimit-reset-requests` headers matching Anthropic's own API header naming; per-IP extraction respects `x-forwarded-for` for reverse-proxy deployments; per-key buckets by the caller's matched `PROXY_API_KEYS` name (or `"default"` when no API keys are configured), so a multi-caller deployment can cap each device/user independently without relying on distinct IPs; all three limits may be active simultaneously; disabled by default; `rateLimitKeyForRequest()` helper exported for unit testing
- `PROXY_SYSTEM_PROMPT` env var — optional operator-defined system prompt prepended to every request; merged before the client's own system field so the client's instructions still take effect; handles string, array-of-blocks, and absent system prompts; propagated to POST /v1/messages, POST /v1/messages/count_tokens, and POST /v1/chat/completions (OpenAI passthrough) so all callers — Claude Code, Cursor, Continue, LiteLLM — get consistent model behavior; token estimates reflect the injected text; startup log prints a truncated preview when set
- `tool_result` image content support — `image` blocks inside `tool_result` messages (e.g., screenshots from computer-use tools) are extracted and appended as a follow-up `user` message with OpenAI multipart image_url format, since OpenAI `role:tool` messages only support string content; text and image follow-up parts are merged into a single user message; vision-capable Ollama models (LLaVA, Qwen2-VL, etc.) can therefore see tool-returned images in multi-turn sessions
- `POST /v1/chat/completions` OpenAI-format passthrough — accepts native OpenAI chat format and pipes it directly to Ollama with no translation; applies full proxy infrastructure (auth, rate-limiting, retry, timeout, client-abort, keepalive SSE comments, request logging, and per-model metrics); `stream:true` is supported and SSE is piped verbatim line-by-line; MODEL_MAP and `OLLAMA_NUM_CTX`/`OLLAMA_KEEP_ALIVE` tuning are applied; makes the proxy a drop-in for any OpenAI-compatible client (Cursor, Continue, LiteLLM, etc.) alongside its existing Anthropic-format support
- `POST /v1/embeddings` OpenAI-format embeddings — forwards to Ollama's `/api/embed` endpoint; accepts `input` as a string or array of strings; returns OpenAI-compatible `{ object:"list", data:[{object:"embedding", embedding:[...], index}], model, usage }` envelope; applies auth, rate-limiting, timeout, retry, client-abort, CORS, and request-id headers same as other endpoints; supports all Ollama embedding models (nomic-embed-text, mxbai-embed-large, etc.) and resolves MODEL_MAP aliases; token usage from `prompt_eval_count` is now recorded via `recordTokens()` and set on `res._logMeta` so embedding requests appear in GET /metrics token totals, per-model breakdown, per-API-key tracking, and request logs — previously these were silently omitted
- `GET /` live dashboard — self-contained HTML status page served by the proxy itself (zero external dependencies); auto-refreshes every 5 seconds; shows Ollama connectivity, active streaming connections, startup config (model, port, auth, TLS, rate limits, context window, etc.), cumulative token usage, per-route request counts, HTTP status code breakdown, p50/p95/p99/min/max/avg latency, and per-model usage; links to `/health`, `/metrics`, `/metrics/prometheus`, and `/v1/models`; also handles `GET /favicon.ico` with 204 to suppress browser console noise
- `OLLAMA_OPTIONS` env var — optional JSON object of arbitrary Ollama model parameters (e.g. `repeat_penalty`, `mirostat`, `mirostat_eta`, `mirostat_tau`, `num_gpu`, `num_thread`, `tfs_z`, `typical_p`) applied to every outbound request; per-request client values take precedence; `OLLAMA_NUM_CTX` and `OLLAMA_KEEP_ALIVE` still take highest precedence; applied to POST /v1/messages, POST /v1/chat/completions, and POST /v1/embeddings; validated at startup with a clear warning if the value is not a JSON object; startup log and live dashboard show the active options; `parseOllamaOptions()` helper exported for unit testing
- `LOG_LEVEL=debug` verbose mode — when set to `debug`, logs the full translated OpenAI-format request body sent to Ollama (via `[DEBUG] → Ollama` lines) and, for non-streaming requests, the raw Ollama response body (`[DEBUG] ← Ollama response`); large base64 image payloads are automatically replaced with a `<base64 N chars>` placeholder via `sanitizeForLog()` so logs stay readable even with vision requests; startup log and dashboard Config card both show the active log level; complementary to `LOG_FORMAT` which controls log-line structure; `sanitizeForLog()` helper exported for unit testing
- `POST /v1/completions` legacy text completions — accepts the OpenAI text completions format (`prompt` string or array, `max_tokens`, `temperature`, `top_p`, `stop`, `seed`, `stream`); converts `prompt` into a single user message and forwards to Ollama's `/v1/chat/completions`; converts the response back to the `text_completion` envelope (`object:"text_completion"`, `choices[].text`); streaming emits SSE deltas in completions format; applies full proxy infrastructure (auth, rate-limiting, retry, timeout, client-abort, keepalive, CORS, metrics, MODEL_MAP, OLLAMA_NUM_CTX/KEEP_ALIVE, PROXY_SYSTEM_PROMPT, OLLAMA_OPTIONS); makes the proxy compatible with older tools and LiteLLM configs that target `/v1/completions`
- Multi-host warmup fix — `PROXY_WARMUP=true` now warms all hosts in `OLLAMA_HOST` in parallel (previously only `OLLAMA_HOSTS[0]` was warmed, leaving all other GPUs in a multi-host deployment cold until their first real request)
- `PROXY_MAX_CONCURRENCY` env var — optional limit on simultaneous in-flight Ollama inference requests (POST /v1/messages, /v1/chat/completions, /v1/completions); when the limit is reached new requests receive 503 `overloaded_error` with `Retry-After: 1`; prevents VRAM OOM errors on single-GPU setups when Claude Code and other callers issue concurrent requests; `active_llm_requests` and `concurrency_limit` fields added to GET /metrics and GET /metrics/prometheus (`proxy_active_llm_requests` + `proxy_concurrency_limit` gauges); in-flight count and limit shown in live dashboard Status and Config cards; startup log always prints current limit or a hint to set it
- `PROXY_MAX_QUEUE_SIZE` / `PROXY_MAX_QUEUE_TIMEOUT` env vars — optional request queue that absorbs bursts when all concurrency slots are taken; instead of immediately returning 503, requests wait in the queue until a slot opens (or until `PROXY_MAX_QUEUE_TIMEOUT` ms elapses, whichever comes first); if the queue itself is full the caller still gets 503 immediately; client disconnect while queued removes the entry without consuming a slot; `queued_llm_requests` and `queue_limit` added to GET /metrics, `proxy_queued_llm_requests` + `proxy_queue_limit` gauges added to GET /metrics/prometheus; queue depth shown in live dashboard Status card alongside in-flight count; works cleanly with multi-host round-robin and PROXY_MAX_CONCURRENCY; also fixes a minor pre-existing bug where `activeLlmRequests` went slightly negative when PROXY_MAX_CONCURRENCY was unset
- `PROXY_HARD_MAX_TOKENS` env var — hard ceiling on per-request output tokens; any `max_tokens` value above this is silently clamped before forwarding to Ollama, preventing one caller from monopolising the GPU with a huge generation budget; also caps the `PROXY_MAX_TOKENS` default so operators only need to set one value; applied to POST /v1/messages, /v1/chat/completions, and /v1/completions; shown in startup log and live dashboard Config card; `resolveMaxTokens()` helper exported for unit testing
- `max_tokens` input validation — POST /v1/messages now validates that `max_tokens`, when provided, is a positive integer; returns 400 `invalid_request_error` with a descriptive message for zero, negative, non-integer float, or non-numeric values; previously negative or zero values were silently forwarded to Ollama which could produce confusing errors; same validation applied to POST /v1/chat/completions and POST /v1/completions
- Model metadata enrichment — GET /v1/models now includes `details` (format, family, families, parameter_size, quantization_level) and `size` (bytes on disk) from Ollama's `/api/tags` response at zero added latency; MODEL_MAP alias entries inherit the same metadata from their target model; model-picker UIs (Continue, Open WebUI, Cursor) can display parameter count and quantization without additional requests
- GET /v1/models/:id enriched with `/api/show` — the single-model lookup now also calls Ollama's `/api/show` endpoint and adds `context_length` (from `model_info.llm.context_length`), `system` (the model's default system prompt, if any), and `template` (the prompt template, if any) to the response; the show call is best-effort and silently skipped if Ollama doesn't return it, so the endpoint stays fast even on older Ollama versions
- DELETE /v1/models/:id — deletes a model from Ollama via `DELETE /api/delete`; auth-gated like other model endpoints; returns `{deleted:true,id}` on success; 404 if the model isn't in Ollama; 502 if Ollama is unreachable; allows operators to manage the model library through the proxy without exposing Ollama's admin port directly; CORS Allow-Methods updated to include DELETE
- `POST /v1/models/pull` — pulls a model from the Ollama registry through the proxy; auth-gated; request body: `{"model":"name[:tag]","stream":false}`; non-streaming returns `{id,pulled:true,object:"model",status}` on success, 422 if Ollama reports the model doesn't exist; streaming (`stream:true`) pipes Ollama's NDJSON progress lines as SSE `data:` events (status, digest, total, completed) followed by a final `{id,pulled:true,done:true}` event so clients can watch download progress in real time; client-abort propagation cancels in-flight pulls immediately; allows operators to pull new models through the proxy API without SSH access to the host
- `POST /v1/messages/batches` — Anthropic Messages Batch API; accepts `{"requests":[{"custom_id":"…","params":{messages,model,max_tokens,…}}]}`, validates each entry, creates an in-memory batch record, and immediately returns a batch object (`id`, `processing_status:"in_progress"`, `request_counts`, `created_at`, `expires_at`); requests are processed serially against Ollama in the background (fire-and-forget) using the same Anthropic→OpenAI conversion as `POST /v1/messages`; auth-gated; `processBatchRequest()` and `processBatch()` helpers exported for unit testing
- `GET /v1/messages/batches` — list all in-memory batches, newest first; returns `{data:[…], has_more:false, first_id, last_id}`; auth-gated
- `GET /v1/messages/batches/{id}` — poll a single batch for current status and `request_counts` breakdown (processing/succeeded/errored/canceled/expired); `results_url` is set once `processing_status` is `"ended"`; 404 if batch not found; auth-gated
- `GET /v1/messages/batches/{id}/results` — streams JSONL (one JSON object per line: `{"custom_id":"…","result":{type,message|error}}`) once the batch has `processing_status:"ended"`; returns 400 if still in progress; format matches Anthropic's batch results spec so SDK clients can parse it directly; auth-gated
- `POST /v1/messages/batches/{id}/cancel` — signals the background processor to stop after the current in-flight request; sets `processing_status:"canceling"` and `cancel_initiated_at`; remaining unprocessed requests receive `{type:"canceled"}`; returns 400 if batch already ended; auth-gated
- Batch expiration enforcement — `processBatch` checks `expires_at` before starting each item and marks any remaining unprocessed requests as `{type:"expired"}` when the batch TTL has passed; a background `cleanupExpiredBatches()` interval (every 5 minutes) also force-expires any in-progress batches that somehow outlive their TTL and removes ended batches from memory once they are more than 1 hour past `ended_at`, preventing unbounded memory growth in long-running deployments; `cleanupExpiredBatches` exported for unit testing
- `PROXY_FORCE_THINK` env var — when set to `true`, unconditionally adds `think:true` to every outgoing Ollama inference request, enabling native chain-of-thought reasoning for thinking models (DeepSeek-R1, Qwen3-thinking, etc.) without requiring the client (Claude Code, Cursor, Continue) to send `thinking:{type:"enabled"}` on each request; safe for non-thinking models — Ollama ignores the field when the model doesn't support it; applied to POST /v1/messages, /v1/chat/completions, /v1/completions, and batch processing; per-request client `thinking` parameters still combine with this baseline; shown in startup log and live dashboard Config card; `PROXY_FORCE_THINK` constant exported for unit testing
- Batch concurrency gating — `processBatch` now acquires a concurrency slot via `acquireLlmSlotForBatch()` before each batch item and releases it via `releaseLlmSlot()` after, so batch background requests respect `PROXY_MAX_CONCURRENCY` and compete fairly with real-time requests rather than bypassing the gate; when all slots are taken the batch item waits in the same `_concurrencyQueue` used by real-time requests; `active_llm_requests` and `queued_llm_requests` metrics (JSON and Prometheus) now accurately reflect batch activity; `acquireLlmSlotForBatch()` exported for unit testing
- `PROXY_LISTEN_HOST` env var — optional bind address (default: all interfaces / `0.0.0.0`); set to `127.0.0.1` to restrict the proxy to localhost-only access, preventing LAN or WAN exposure without needing a firewall rule; shown in startup log and live dashboard Config card; useful for single-machine setups where only the local user should reach the proxy
- Startup Ollama connectivity check — immediately after the proxy starts listening, a non-blocking background check pings all configured Ollama hosts and logs a clear warning (with `ollama serve` remediation hint) if any are unreachable; prevents silent first-request 502 failures when Ollama hasn't started yet; skipped when `PROXY_WARMUP=true` because the warmup request already verifies reachability as part of model pre-loading
- `X-Accel-Buffering: no` on all SSE streaming responses — tells nginx (and any nginx-compatible reverse proxy) not to buffer the event stream; without this header nginx silently accumulates SSE chunks until the response completes, causing Claude Code and other streaming clients to hang until the entire generation finishes rather than seeing tokens as they arrive; applied to `POST /v1/messages`, `POST /v1/chat/completions`, `POST /v1/completions`, and `POST /v1/models/pull` streaming paths; ignored by non-nginx deployments (CDNs, HAProxy, direct connections) so there is no downside to setting it unconditionally
- `anthropic-version: 2023-06-01` response header on all `/v1/messages*` routes — mirrors what the real Anthropic API returns; the official Anthropic TypeScript SDK (and some strict validation middleware) checks for this header and may reject responses that omit it; covers `POST /v1/messages`, `POST /v1/messages/count_tokens`, and all batch routes (`/v1/messages/batches*`)
- `PROXY_AUTO_TRUNCATE` env var — when `true` and `OLLAMA_NUM_CTX` is set, the proxy automatically drops the oldest user/assistant turns from the message history before forwarding to Ollama whenever the estimated input token count would exceed `OLLAMA_NUM_CTX`; prevents the "context length exceeded" 400 errors that hit long Claude Code sessions against small-context Ollama models; token count estimated via chars/4 heuristic; always preserves the system message and at least the last 2 messages; after each removal advances past any orphaned tool/assistant messages at the new head so the remaining history stays valid OpenAI format; sets `x-context-truncated: N` response header when truncation occurs; applied to `POST /v1/messages`, `POST /v1/chat/completions`, and batch items; opt-in (default false) so existing deployments see no behaviour change; `truncateToContext()` helper exported for unit testing
- `PROXY_API_KEYS` env var — multi-caller API key support; comma-separated list of `name:key` pairs (bare keys without a colon are auto-named `key1`, `key2`, ...) that combine with the existing `PROXY_API_KEY` (always labeled `default`); `checkAuth()` matches the caller's key against every configured key without early-exit so timing doesn't leak which entry matched, and tags `req._apiKeyName` with the matched key's name; per-key usage (`requests`, `tokens_in`, `tokens_out`) is tracked in `_metrics.apiKeysUsed` and exposed via `GET /metrics` (`api_keys_usage`), `GET /metrics/prometheus` (`proxy_api_key_requests_total{key_name}` / `proxy_api_key_tokens_total{key_name,direction}`), and a dashboard "API Key Usage" card (shown only when more than one key is configured); lets an operator hand out separate keys per device/user, monitor usage per key, and revoke a single key without rotating the shared secret; startup log lists configured key names (never the key values); `parseApiKeys()` helper exported for unit testing
- `PROXY_BATCH_PERSIST_PATH` env var — persists the Messages Batch API's in-memory `_batches` store to a JSON file after every state change (batch creation, each item result, cancellation, and TTL/cleanup expiry) so batches and their results survive a proxy restart; `saveBatchesToDisk()` serializes each batch's `results` Map to an array of entries and writes are serialized (a save-in-progress flag queues one follow-up save) so concurrent updates can't interleave and corrupt the file; `loadBatchesFromDisk()` runs at startup, reconstructs each batch's `results` Map, and automatically resumes (via `processBatch`) any batch that hadn't reached `'ended'` status when the proxy last stopped; `processBatch` now skips any item that already has a recorded result, so resumed batches continue from where they left off instead of reprocessing finished items; opt-in (default unset — in-memory only, no behaviour change); shown in startup log and the dashboard Config card; `saveBatchesToDisk()` and `loadBatchesFromDisk()` exported for unit testing
- Mid-stream error chunk handling — Ollama can emit a `{"error": ...}` chunk after a streaming response has already started (e.g. the model crashes or runs out of VRAM mid-generation); previously such a chunk has no `choices` array and was silently skipped, so the stream ended as if it had completed normally (`stop_reason: 'end_turn'` with a normal `message_stop`), hiding the failure from the client; `POST /v1/messages` now detects this chunk, closes any open thinking/text/tool_use content blocks via a shared `closeAllBlocks()` helper (also used by the existing finish_reason and no-finish_reason-fallback paths), and emits an Anthropic `error` SSE event (`type: 'api_error'`) with the underlying message instead of a misleading `message_delta`/`message_stop`; `POST /v1/completions` passes the `{"error":...}` chunk straight through to the caller; `POST /v1/chat/completions` (OpenAI passthrough) already forwarded such chunks verbatim and needed no change
- Multi-host automatic failover — `getOllamaHost()` now tracks per-host health and skips a host that has failed `HOST_UNHEALTHY_THRESHOLD` (2) consecutive checks, so a crashed Ollama instance or unplugged GPU box in an `OLLAMA_HOST` round-robin deployment no longer eats every Nth request with guaranteed-to-fail retries; a single successful check immediately restores the host; if every host is unhealthy the proxy fails open and rotates normally so existing per-request retry/error handling still applies; in multi-host deployments (`OLLAMA_HOSTS.length > 1`) a background interval pings each host's `/api/tags` every 15 s, seeded immediately at startup; `GET /health` performs its own live check on every call (via the same `checkHostHealth()`) and now includes a `routing: "active"|"skipped"` field per host showing whether it's currently eligible for round-robin selection, also reflected as a "skipped" badge in the live dashboard's Status card; `checkHostHealth()`, `recordHostHealth()`, `_hostHealth`, and `HOST_UNHEALTHY_THRESHOLD` exported for unit testing
- `POST /v1/messages/count_tokens` now validates that `messages` is present and is an array, returning a 400 `invalid_request_error` instead of a 500, matching the validation already applied to `POST /v1/messages`; also fixed `toOpenAIMessages()` to fall back to empty string content (instead of an invalid empty array) when an image content block has an unsupported/missing `source` (e.g. a Files API `{type:"file"}` reference) and the message has no other text
- `GET /health` model availability check — `checkHostHealth()` now also stashes each host's `/api/tags` model list in `_hostHealth[url].models`; `GET /health` cross-references `OLLAMA_MODEL` and every `MODEL_MAP` target (via `getConfiguredModelNames()`) against the union of models available across all reachable hosts and adds `model_available` (`true`/`false`/`null` when unknown), `models_status` (per-model breakdown, present when more than one model is configured), and a `warning` field naming any missing models with an `ollama pull` hint; `status` becomes `"degraded"` when a configured model is missing even if Ollama itself is reachable, catching the common misconfiguration of pointing the proxy at a model that hasn't been pulled yet; the live dashboard's Status card shows a "Model — not pulled ⚠" warning in this case; `getConfiguredModelNames()` exported for unit testing
- `model` field type validation — `POST /v1/messages`, `/v1/chat/completions`, `/v1/completions`, `/v1/embeddings`, `/v1/messages/count_tokens`, and each item in `POST /v1/messages/batches` now return a 400 `invalid_request_error` ("`model` must be a string") if the client sends a non-string `model` value (number, boolean, array, object); previously this reached `resolveModel()`, whose `String.prototype.startsWith()` call threw a `TypeError` that surfaced as an opaque 500 `internal_error`; `resolveModel()` itself is also now defensive — non-string input falls back to the configured default model instead of throwing, so any future caller is protected even without explicit validation; `validateModelField()` exported for unit testing
- `PROXY_API_KEY_MODELS` env var — per-API-key model allow-list for multi-caller deployments; comma-separated list of `name:model1|model2|...` entries (the name matches a `PROXY_API_KEYS`/`PROXY_API_KEY` name, or `"default"` when no named keys are configured); models are pipe-separated since Ollama model names themselves contain colons (e.g. `qwen2.5:7b`), e.g. `family:llama3.2:1b,kids:llama3.2:1b|qwen2.5:7b`; `parseApiKeyModels()` parses this into a `Map<name, Set<model>>` (`_apiKeyModels`); `checkModelAccess(req, effectiveModel)` looks up the caller's key name (via `req._apiKeyName`, falling back to `"default"`) and returns `null` if the key has no entry (unrestricted) or the model is allowed, otherwise a descriptive error string; called after `resolveModel()` in `POST /v1/messages`, `/v1/chat/completions`, `/v1/completions`, `/v1/embeddings`, and for each request in `POST /v1/messages/batches` (at creation time, before any background processing starts) — a disallowed model returns `403 permission_error`; the check runs after `MODEL_MAP` alias resolution so the restriction applies to the real Ollama model name; startup log lists each restricted key's allowed models (never exposed for unrestricted keys); the dashboard Config card shows an info badge per restricted key listing its allowed models on hover; `parseApiKeyModels()`, `checkModelAccess()`, and `_apiKeyModels` exported for unit testing
- Fixed two long-standing syntax errors in the `GET /` live dashboard's embedded `<script>` that made the entire script fail to parse in the browser (so the dashboard never loaded any live data — `refresh()` never ran): the `row()` helper had a stray `'")>'` instead of `(cls||'')+'">'`, and `hh.url.replace(/^https?:\/\//,'')` / `C.hosts[0].replace(/^https?:\/\//,'')` had their `\/` sequences collapsed by the outer template literal into `///`, turning the rest of the line into a comment. Both are escaping bugs in proxy.js's outer template literal — fixed by writing `\\/\\/ ` so the emitted client-side regex correctly reads `\/\/ `
- `GET /v1/models` and `GET /v1/models/:id` now respect `PROXY_API_KEY_MODELS` — `isModelVisibleToCaller(req, realModelName)` wraps `checkModelAccess()` to decide whether a caller's key may see a given Ollama model; `handleModels` filters both the real Ollama model list and `MODEL_MAP` aliases (an alias is shown only if its target model is visible) so restricted keys (e.g. a "kids" key limited to `llama3.2:1b`) only see models they can actually use; `handleModelById` returns the same `404 not_found_error` for a model outside the caller's allow-list as for a model that doesn't exist in Ollama, avoiding confirmation of restricted models' existence; keeps model-picker UIs (Continue, Open WebUI, Cursor) from offering choices that would 403; `isModelVisibleToCaller()`, `handleModels`, and `handleModelById` exported for unit testing
- `system` field type validation — `POST /v1/messages`, `POST /v1/messages/count_tokens`, and each item in `POST /v1/messages/batches` now return a 400 `invalid_request_error` ("`system` must be a string or an array of content blocks") if the client sends a non-string, non-array `system` value (number, boolean, object); previously a truthy non-array `system` reached `injectSystemPrompt()`'s `...system` spread whenever `PROXY_SYSTEM_PROMPT` was configured, throwing a `TypeError` that surfaced as an opaque 500 `internal_error`; `validateSystemField()` exported for unit testing
- `messages` entry validation — `POST /v1/messages`, `POST /v1/chat/completions`, `POST /v1/messages/count_tokens`, and each item in `POST /v1/messages/batches` now return a 400 `invalid_request_error` if any entry in `messages` is `null`/non-object, has a non-string `role`, or has a `content` that is neither a string nor an array of objects with a string `type` (including the nested `content` array of a `tool_result` block); previously a malformed entry such as `messages: [null]` or a `null` content block reached `toOpenAIMessages()`'s `.content`/`.type` property accesses (or, for the OpenAI passthrough, the system-prompt-injection/auto-truncate code's `.role` accesses), throwing a `TypeError` that surfaced as an opaque 500 `internal_error`; `validateMessages()` exported for unit testing
- `DELETE /v1/messages/batches/{id}` — completes the Anthropic Message Batches CRUD surface; deletes a batch once it has finished (`processing_status: "ended"`), returning `{id, type:"message_batch_deleted"}`; matches the real API's restriction that an in-progress or canceling batch must be canceled (and allowed to finish) before it can be deleted — returns 400 `invalid_request_error` otherwise, and 404 `not_found_error` for an unknown batch id; deletion is persisted immediately via `saveBatchesToDisk()` when `PROXY_BATCH_PERSIST_PATH` is set; auth-gated; `handleDeleteBatch()` exported for unit testing
- Per-API-key batch isolation — every batch created via `POST /v1/messages/batches` is now tagged with an `owner` field (the caller's `req._apiKeyName`, or `"default"` when no named key matched, via `batchOwnerName()`); `GET /v1/messages/batches` only lists batches owned by the calling key, and `GET /v1/messages/batches/{id}`, `GET /v1/messages/batches/{id}/results`, `POST /v1/messages/batches/{id}/cancel`, and `DELETE /v1/messages/batches/{id}` return `404 not_found_error` (rather than confirming existence) for a batch owned by a different key — closing a gap in multi-caller (`PROXY_API_KEYS`) deployments where any authenticated caller could previously list, read the results of, cancel, or delete any other caller's batches; batches persisted before this change have no `owner` field and are treated as belonging to `"default"`, so existing single-key/no-auth deployments are unaffected; `batchOwnedByCaller()` and `batchOwnerName()` exported for unit testing
- `max_tokens` validation in batch creation — `POST /v1/messages/batches` now validates `max_tokens` per request item at creation time (zero, negative, non-integer float, or non-numeric values return 400 `invalid_request_error` immediately, with the offending `custom_id` named in the error message); previously an invalid `max_tokens` was accepted at creation time and only discovered as an `errored` result after background processing ran, giving no actionable feedback until the batch ended; matches the validation already applied to `POST /v1/messages`, `/v1/chat/completions`, and `/v1/completions`; omitting `max_tokens` still falls back to `PROXY_MAX_TOKENS` (default 8192)
- `GET /v1/messages/batches` pagination — now supports the real API's `limit` (default 20, 1-1000), `before_id`, and `after_id` query params instead of always returning every batch the caller owns in one response with `has_more` hardcoded to `false`; results stay ordered newest-first, `first_id`/`last_id` now describe the returned page (not the full list) so the official Anthropic SDK's auto-paginating iterators work correctly against the proxy; an out-of-range `limit` returns 400 `invalid_request_error`; a `before_id`/`after_id` that doesn't resolve to one of the caller's own batches (including one that belongs to a different `PROXY_API_KEYS` caller) is treated as an empty page rather than erroring, so a cursor can never be used to probe whether an id exists in another tenant's batch list; `parseBatchListParams()` exported for unit testing
- `PROXY_API_KEY_MODELS` now enforced on model-management write endpoints — `DELETE /v1/models/:id` and `POST /v1/models/pull` previously checked only `checkAuth()` and ignored each caller's model allow-list, so a restricted key (e.g. a "kids" key limited to `llama3.2:1b`) could delete any model on the shared Ollama instance — including ones other callers depend on — or pull arbitrary new models into shared disk/bandwidth, even though that same key was already blocked from running inference with those models; both handlers now call `checkModelAccess(req, modelId)` (delete) / `checkModelAccess(req, pullReq.model)` (pull) before touching Ollama and return `403 permission_error` for a disallowed model, matching the enforcement already applied to `POST /v1/messages`, `/v1/chat/completions`, `/v1/completions`, `/v1/embeddings`, and `GET /v1/models*`
- `PROXY_API_KEY_MODELS` now enforced on `POST /v1/messages/count_tokens` — this was the one remaining model-touching endpoint that resolved the requested model and forwarded it straight to Ollama's `/api/tokenize` without ever calling `checkModelAccess()`, unlike every other endpoint that touches a model (`/v1/messages`, `/v1/chat/completions`, `/v1/completions`, `/v1/embeddings`, batches, and the model-management routes); a restricted key (e.g. a "kids" key limited to `llama3.2:1b`) could exploit this to force Ollama to load a disallowed model into memory purely to count tokens, even though it was blocked from ever generating with that model; `handleCountTokens` now calls `checkModelAccess(req, effectiveModel)` right after `resolveModel()` and returns `403 permission_error` (without contacting Ollama) for a disallowed model, exactly mirroring the existing checks elsewhere; `handleCountTokens` exported for unit testing
- `OLLAMA_HOST` trailing-slash normalization — every Ollama call site builds URLs as `` `${host}/api/...` `` or `` `${host}/v1/...` ``, so a host configured with a trailing slash (e.g. `OLLAMA_HOST=http://localhost:11434/`, easy to paste in from a browser address bar or a `.env` file) previously produced a double-slash path like `http://localhost:11434//api/tags` on every single request — health checks, inference, embeddings, model management, everything; double slashes are not guaranteed to resolve the same as the intended single-slash path on every HTTP server/router, so this could silently break an entire deployment with no clear error message pointing at the cause; `OLLAMA_HOSTS` parsing now strips one or more trailing slashes from each comma-separated entry (`http://host///` → `http://host`) before the array is used anywhere, so this footgun is fixed at the single point all hosts are parsed rather than needing a fix at every call site
- `POST /v1/embeddings` now applies `OLLAMA_NUM_CTX` and `OLLAMA_KEEP_ALIVE` — every other inference endpoint (`/v1/messages`, `/v1/chat/completions`, `/v1/completions`) sets `num_ctx`/`keep_alive` on its outgoing Ollama request, but `handleEmbeddings` only forwarded `OLLAMA_OPTIONS` and silently dropped these two dedicated env vars; an operator who set `OLLAMA_KEEP_ALIVE=-1` to keep models resident in GPU memory would still see their embedding model evicted on Ollama's default 5-minute keep-alive between requests, and `OLLAMA_NUM_CTX` had no effect on embedding input truncation; `handleEmbeddings` now builds its request body the same way as the other endpoints (`OLLAMA_OPTIONS` spread first, then `num_ctx`/`keep_alive` applied when set, taking precedence same as elsewhere)
- Batch token usage now attributed to the owning API key — `processBatchRequest()` (called from `processBatch()` for every item in `POST /v1/messages/batches`) calls `recordTokens()` to update the global and per-model usage counters but previously omitted the `apiKeyName` argument, so background batch processing never appeared in `_metrics.apiKeysUsed` / `GET /metrics` `api_keys_usage` / `GET /metrics/prometheus` `proxy_api_key_tokens_total`, even though batches already track an owning key via the `owner` field (added for per-API-key batch isolation); a multi-caller deployment (`PROXY_API_KEYS`) monitoring or budgeting per-key usage would silently undercount any caller who used the Batch API instead of real-time requests; `processBatchRequest()` now accepts an `apiKeyName` parameter and `processBatch()` passes `batch.owner || 'default'` (matching the same fallback `batchOwnedByCaller()` already uses for batches persisted before the `owner` field existed) so batch-processed tokens are now correctly attributed
- Fixed `RATE_LIMIT_PER_IP_RPM` x-forwarded-for spoofing — `getClientIp()` previously trusted the client-supplied `x-forwarded-for` header unconditionally, but `PROXY_LISTEN_HOST` defaults to all interfaces (`0.0.0.0`), meaning most deployments are reached directly rather than through a reverse proxy; any direct caller could send a different `x-forwarded-for` value on every request to get a fresh rate-limit bucket and fully bypass `RATE_LIMIT_PER_IP_RPM`, the one limit specifically meant to stop a single misbehaving client from hogging the GPU; `getClientIp()` now only consults `x-forwarded-for` when the new `PROXY_TRUST_PROXY` env var is `true`, otherwise it always uses the raw socket address (which a client cannot spoof); default is `false` (secure by default) — operators who do run behind a reverse proxy/load balancer that overwrites `x-forwarded-for` with the real client IP must opt in with `PROXY_TRUST_PROXY=true` to restore the previous IP-extraction behavior; shown in the startup log and the live dashboard's per-IP rate-limit row
- Fixed leaked `PROXY_TIMEOUT` timer and socket `close` listener on oversized-body rejection — `handleMessages`, `handleEmbeddings`, `handleOpenAIChat`, and `handleOpenAICompletions` each register a per-request timeout timer and a `req.socket.once('close', onClientClose)` listener before calling `readBody(req)`; when `readBody()` throws (most commonly a `PAYLOAD_TOO_LARGE` rejection from `PROXY_MAX_BODY_SIZE`), the throw was caught only by the generic router-level `try/catch`, which has no access to those handler-local closures, so the timer kept running and the listener stayed attached; if the timer fired afterward it logged a misleading "Request timeout" message for a request that had actually already failed for an unrelated reason (an oversized payload), and the listener leaked until the socket closed naturally; each handler now wraps its `readBody()` call in a local try/catch that removes the listener and clears the timer (`req.socket.off('close', onClientClose); clearTO();`) before rethrowing, so the router's existing 413/500 mapping is unchanged but cleanup always runs; covered by a new test.js suite (`describe('readBody failure cleanup ...')`) that exercises all four handlers with a real `EventEmitter`-based mock socket and asserts `listenerCount('close')` drops to zero after the rejection
- Fixed `processBatch` pinning a whole batch to a single Ollama host — `processBatch()` previously called `getOllamaHost()` once at the top of the function, before the `for (const item of batch.requests)` loop, so every item in a `POST /v1/messages/batches` run hit the same host for the batch's entire lifetime (potentially hours); every other handler (`handleMessages`, `handleOpenAIChat`, `handleOpenAICompletions`, `handleEmbeddings`, `handleModels`, `handleCountTokens`, etc.) already calls `getOllamaHost()` fresh per request, so batches alone defeated both `OLLAMA_HOST` round-robin load distribution across multiple GPUs/instances and the automatic-failover health tracking (`_hostHealth`) that lets the proxy route around a host that goes down mid-batch; `getOllamaHost()` is now called inside the loop, once per item, right after `acquireLlmSlotForBatch()`, matching the per-request pattern used everywhere else; covered by a new test.js suite (`describe('processBatch — multi-host round robin')`) using the existing `withHosts()` test helper, which asserts consecutive batch items are dispatched to different hosts in round-robin order
- Hardened non-streaming tool-call response handling in `handleMessages` and `processBatchRequest` — a malformed Ollama response whose `choices[0]` has no `message` field (e.g. a future Ollama version or a misbehaving custom backend) previously reached `choice.message.content`/`choice.message.tool_calls` unguarded, throwing a `TypeError` that surfaced as an opaque 500 `internal_error` instead of the existing clean 502 `ollama_error` used for the sibling "empty choices array" case; the `!choice` guard in both functions is now `!choice || !choice.message`. Separately, when a model emits non-JSON `tool_calls[].function.arguments` (common with smaller local models, which are less reliable than Claude at strict JSON tool-call formatting), the proxy already defaulted to an empty `{}` input but did so silently, giving operators no way to tell — short of enabling full `LOG_LEVEL=debug` — why a tool call reached the client with empty arguments; both call sites now log a `[tool-call]` warning naming the tool and the raw malformed string before falling back to `{}`; covered by new tests in `test.js` for both the `handleMessages` non-streaming path and `processBatch`/`processBatchRequest`
- `POST /v1/embeddings` now honors the OpenAI `encoding_format` field — the real OpenAI Embeddings API returns each vector as a base64 string of little-endian float32 bytes when the caller sends `encoding_format:"base64"`, and several popular OpenAI-compatible clients (notably LangChain's `OpenAIEmbeddings`, which defaults to requesting `"base64"` for faster numpy decoding) rely on this; `handleEmbeddings` previously ignored the field entirely and always returned a plain JSON float array, so any client requesting base64 would receive a JS array where it expected a base64 string and fail downstream (e.g. `base64.b64decode()` on a list, or a numpy dtype mismatch) with no error from the proxy itself; `validateEncodingFormat()` rejects any value other than `"float"`/`"base64"`/absent with a 400 `invalid_request_error` (mirroring the existing `validateModelField` pattern), and `embeddingToBase64()` encodes each embedding vector as a base64 string of `Float32Array` bytes — matching the real API's wire format byte-for-byte — when `encoding_format:"base64"` is requested; omitted or `"float"` is unchanged (plain array); `validateEncodingFormat()` and `embeddingToBase64()` exported for unit testing
- Fixed unguarded `ollamaRes.text()` reads in the `!ollamaRes.ok` error path of `handleMessages`, `handleCountTokens`, `handleOpenAIChat`, and `handleOpenAICompletions` — `handleMessages` read the Ollama error body *before* calling `req.socket.off('close', onClientClose)`/`clearTO()`, the exact leaked-timer/listener bug class already fixed elsewhere (see the `readBody` cleanup fix above) but missed for this one code path: if the read throws (client aborts mid-read, decoding failure) the per-request timeout timer and socket `close` listener never get cleaned up, and the exception escapes to the router's generic catch as an opaque 500 instead of the proper mapped Ollama error. Cleanup now runs first, matching every sibling handler, and all four call sites use `.text().catch(() => '')` (matching the pattern already used at the model-delete/pull call sites) so a failed body read degrades to an empty string instead of throwing; covered by two new `test.js` cases in `describe('handleMessages')` asserting (1) the response still resolves to a mapped error status when `.text()` rejects, and (2) the close listener is removed (`listenerCount('close') === 0`) after a non-ok Ollama response
- `RATE_LIMIT_RPM`/`RATE_LIMIT_PER_IP_RPM`/`RATE_LIMIT_PER_KEY_RPM` now enforced on `POST /v1/messages/batches` — batch creation was the one remaining endpoint that triggers Ollama inference (each batch item is processed through the same `acquireLlmSlotForBatch()`/Ollama call path as real-time requests) yet was never gated by any of the three request-rate limits, unlike `/v1/messages`, `/v1/chat/completions`, `/v1/completions`, `/v1/embeddings`, and `/v1/messages/count_tokens`; a caller could fully bypass the global/per-IP/per-key request caps — whose documented purpose is specifically to stop one caller from exhausting the shared GPU — by submitting work through the Batch API instead of real-time calls, since a single `POST /v1/messages/batches` call can enqueue an arbitrary number of inference requests for background processing; the router now applies the same three `checkRateLimit()` calls used by the other rate-limited routes, after `checkAuth()` and before `handleCreateBatch()`, so repeated batch-creation calls now consume the same budget as real-time requests; covered by new `test.js` cases asserting each of the three limits returns `429 rate_limit_error` for batch creation once exceeded
- `RATE_LIMIT_RPM`/`RATE_LIMIT_PER_IP_RPM`/`RATE_LIMIT_PER_KEY_RPM` now enforced on `POST /v1/models/pull` — this was the last unguarded endpoint capable of consuming significant shared resources without limit: unlike the inference endpoints (which also have `PROXY_MAX_CONCURRENCY` as a backstop), a model pull has *no* other safeguard at all, and a single call can download many gigabytes from the Ollama registry to shared disk/bandwidth; an authenticated caller (or any caller, on a deployment with no `PROXY_API_KEY` configured) could previously issue pulls back-to-back with no throttling whatsoever, unlike every other mutating/inference-triggering route; the router now applies the same three `checkRateLimit()` calls used elsewhere, after `checkAuth()` and before `handlePullModel()`; covered by new `test.js` cases (`describe('rate limiting on POST /v1/models/pull')`) asserting each of the three limits returns `429 rate_limit_error` for a pull request once exceeded, and that pulls still succeed normally when no rate limit env vars are set
- `validateSystemField()` now validates the elements of a `system` array, not just its top-level type — `toOpenAIMessages()` reads `.type`/`.text` off each element of an array `system` unguarded (e.g. `system: [null]`), and previously only the array-vs-string-vs-other top-level check existed, so a malformed element threw a `TypeError` that surfaced as an opaque 500 `internal_error` on `POST /v1/messages` and `POST /v1/messages/count_tokens` instead of the clean 400 `invalid_request_error` already given for a malformed top-level `system` value or a malformed `messages` content block; each array element must now be a non-null, non-array object with a string `type`, matching the same per-element check `validateMessages()` already applies to message content blocks; verified live against a running proxy with `system:[null]`; covered by new `test.js` cases in `describe('validateSystemField')`
- Fixed a stored-XSS vulnerability in the `GET /` live dashboard — its embedded client-side `<script>` renders `/health` and `/metrics` data into the page via `innerHTML` with no escaping; two of those fields are attacker-controlled: `models_usage` keys come straight from the client-supplied `model` field (`resolveModel()` passes any unrecognized model name through verbatim), and `requests_total` keys come from the raw, unvalidated `req.url` path recorded for *every* request — including unauthenticated 404s, before any `checkAuth()` runs. A caller could therefore plant a payload like `model:"<img src=x onerror=...>"` on a single inference request, or simply request a path like `/<svg onload=...>`, and have it execute in the operator's browser the next time they opened the dashboard, no authentication required for the path-based vector. Added a client-side `esc()` HTML-escaping helper (there was previously no HTML escaper in the file at all — only a Prometheus label-value escaper) and applied it at every untrusted-data injection site: route keys in the Requests card, model names and API-key names in their usage cards, and the Ollama host/connectivity error messages. `handleDashboard` exported for unit testing; new `test.js` `describe('GET / dashboard — XSS escaping')` suite runs the actual embedded script in a `node:vm` sandbox with a mocked DOM/fetch and malicious payloads to verify escaping holds at runtime (confirmed these tests fail against the unpatched script, proving they exercise the real vulnerability)
- `MAX_BATCH_REQUESTS` cap (100,000) on `POST /v1/messages/batches` — `handleCreateBatch` previously accepted a `requests` array of any length, validating and then permanently retaining every item's full `messages`/`tools`/`system` payload in the in-memory `_batches` Map (and, if `PROXY_BATCH_PERSIST_PATH` is set, writing the whole thing to disk on every state change) for up to an hour after the batch ends; a single unauthenticated-by-size call could submit an arbitrarily large `requests` array and exhaust proxy memory/disk well before `PROXY_MAX_BODY_SIZE` (which is unset by default, and even when set bounds total bytes, not item count) would reject it, unlike every other resource a single caller can consume (rate limits, concurrency limits, body-size limits, output-token limits all already existed for the real-time endpoints). The real Anthropic Message Batches API caps a batch at 100,000 requests; enforcing the same documented limit here closes the gap and keeps proxy behavior aligned with the API it emulates — a batch over the limit is rejected with `400 invalid_request_error` before any per-item validation or batch creation occurs, matching the existing empty-array check it sits next to. `MAX_BATCH_REQUESTS` exported for unit testing; covered by new `test.js` cases in `describe('handleCreateBatch — requests array size limit')` asserting the limit rejects an oversized batch without creating it, and that a batch at exactly the limit is still accepted (boundary checked with a stubbed `processBatch` to avoid actually running 100,000 fake inference calls)
- `RATE_LIMIT_RPM`/`RATE_LIMIT_PER_IP_RPM`/`RATE_LIMIT_PER_KEY_RPM` now enforced on `DELETE /v1/models/:id` — this was the one remaining model-management mutating endpoint missing rate-limit enforcement: `POST /v1/models/pull` already gates on all three limits (added in an earlier fix specifically because a pull can download many gigabytes with no other safeguard), but the router's `DELETE /v1/models/:id` branch went straight from `checkAuth()` to `handleDeleteModel()` with no `checkRateLimit()` calls at all, leaving an authenticated caller free to issue unlimited delete calls against the shared Ollama instance — including repeatedly deleting and re-querying models other callers depend on — while every sibling mutating endpoint (`/v1/messages/batches`, `/v1/models/pull`) already had this protection; the router now applies the same three `checkRateLimit()` calls used by `POST /v1/models/pull`, after `checkAuth()` and before `handleDeleteModel()`; covered by new `test.js` cases (`describe('rate limiting on DELETE /v1/models/:id')`) asserting each of the three limits returns `429 rate_limit_error` for a delete request once exceeded, and that deletes still succeed normally when no rate limit env vars are set
- Fixed silent data loss for unrecognized Anthropic content block types in `toOpenAIMessages()` — the function only ever recognized six block types (`text`, `image`, `tool_use`, `tool_result`, `thinking`, `document`); any other type — `redacted_thinking` (emitted whenever extended thinking is redacted for safety), `server_tool_use`/`web_search_tool_result` (Claude's built-in tools), or any block type the real Anthropic API adds in the future — was silently filtered out everywhere it mattered (`textParts`, `imageParts`, `docParts`, `thinkParts`, `toolUses`, `toolResults` all skip unknown `type` values), so a client replaying conversation history containing one of these blocks would have that turn's content vanish with zero indication anything was lost; a new module-level `KNOWN_CONTENT_BLOCK_TYPES` set is checked for every block, logging a `[content-block]` warning naming the dropped type and message role, and `textParts` now also picks up any unknown block that carries a string `text` field (a common forward-compatible shape) so at least textual fidelity survives instead of the block disappearing outright; covered by new `test.js` cases in `describe('toOpenAIMessages')` asserting (1) a `redacted_thinking` block is dropped with a warning naming it, (2) an unknown block with a `text` field is folded into the message's text content with a warning noting the fallback, and (3) the same text-fallback also surfaces correctly inside a `tool_result` turn's follow-up user message
- `MAX_METRICS_PATH_KEYS` cap (200) on `_metrics.requests` — the `finish` listener that calls `recordRequest()` is registered in `requestHandler` before `checkAuth()` runs, so it fires for every request including unauthenticated ones, and the metrics key is built from the raw, attacker-controlled `req.url` path (only the query string is stripped); a caller with no API key at all could previously flood the proxy with requests to an arbitrary number of unique paths (`/a`, `/b`, `/c`, ...) and grow `_metrics.requests` — and therefore the size of every `GET /metrics`/`GET /metrics/prometheus` response — without bound for the life of the process, the same unbounded-growth bug class as the `MAX_BATCH_REQUESTS` cap above but on the metrics store instead of the batch store (the dashboard XSS fix escaped this same attacker-controlled field when rendering it, but never addressed the underlying unbounded storage); `recordRequest()` now keeps incrementing a path's own key once it already has one, allows new keys only while fewer than `MAX_METRICS_PATH_KEYS` distinct keys exist, and buckets every path beyond that cap under a per-method `"METHOD (other)"` overflow key instead of creating a new entry; the real route table is a small fixed set so this never triggers during normal operation; `recordRequest` and `MAX_METRICS_PATH_KEYS` exported for unit testing; covered by new `test.js` cases in `describe('recordRequest — _metrics.requests cardinality cap')` asserting the cap holds under a flood of distinct paths, that a path seen before the cap was reached keeps its own key, and that status-code/latency tracking is unaffected while paths are being bucketed
- Fixed `handleCreateBatch` crashing with an opaque 500 on a malformed entry in `requests` — the per-item validation loop in `POST /v1/messages/batches` dereferenced `r.custom_id` immediately, with no guard that `r` itself was a non-null, non-array object first; a body like `{"requests":[null]}` therefore threw an uncaught `TypeError` (`Cannot read properties of null (reading 'custom_id')`) that escaped to the router's generic catch instead of the clean `400 invalid_request_error` every sibling validation in this same function (`validateMessages()`, `validateSystemField()`, `validateTools()`, `resolveMaxTokens()`) already produces for malformed input — the same per-element null/array guard `validateMessages()` already applies to message entries was simply never applied to the top-level batch-request entries themselves; the loop now rejects any entry that is `null`, not an object, or an array with `400 invalid_request_error` before touching `custom_id` or `params`; covered by new `test.js` cases in `describe('handleCreateBatch — malformed requests array entries')` asserting a `null`, string, or array entry all return 400 without creating a batch, and that a valid entry following a malformed one is never processed
- `RATE_LIMIT_RPM`/`RATE_LIMIT_PER_IP_RPM`/`RATE_LIMIT_PER_KEY_RPM` now enforced on the five batch sub-routes — `GET /v1/messages/batches` (list), `GET /v1/messages/batches/{id}` (get), `GET /v1/messages/batches/{id}/results`, `POST /v1/messages/batches/{id}/cancel`, and `DELETE /v1/messages/batches/{id}` previously called only `checkAuth()` in the router, with none of the three `checkRateLimit()` calls already applied to every sibling mutating/resource-touching route (`POST /v1/messages/batches` batch creation, `POST /v1/models/pull`, `DELETE /v1/models/:id`); an authenticated caller could fully bypass an operator's configured request-rate limits by hitting these batch sub-routes instead of the rate-limited ones — `GET .../results` in particular can synchronously write up to `MAX_BATCH_REQUESTS` (100,000) JSONL lines per call with no throttle at all. The router now applies the same three `checkRateLimit()` calls used everywhere else to all five routes, after `checkAuth()` and before the corresponding handler; covered by new `test.js` cases in `describe('rate limiting on batch sub-routes (list/get/results/cancel/delete)')` asserting `RATE_LIMIT_RPM` returns `429 rate_limit_error` on a second call to each of the five routes, that `RATE_LIMIT_PER_IP_RPM`/`RATE_LIMIT_PER_KEY_RPM` are also wired (checked on the list route), and that all five routes still succeed normally when no rate limit env vars are set
- Fixed silent data loss in the `PROXY_SYSTEM_PROMPT` injection for `POST /v1/chat/completions` (OpenAI passthrough) — `handleOpenAIChat` merges the operator-configured system prompt into an existing `role:"system"` message by reading its `content`, but previously treated any non-string `content` as `''`, discarding it outright; a client sending the valid OpenAI array-of-content-parts shape (e.g. `content:[{type:"text",text:"..."}]`, used by several OpenAI SDK clients) would have that entire system message silently replaced by just the injected prompt, with no error or warning — the Anthropic path's `injectSystemPrompt()` already handled its array-of-blocks equivalent correctly by prepending a block, but this OpenAI-passthrough code path was never updated to match. `content` is now branched on string vs. array vs. other: a string still gets the prompt prepended as text, an array gets a `{type:"text",text:...}` part prepended (preserving the original parts, mirroring `injectSystemPrompt()`'s approach), and any other truthy value falls back to just the injected prompt; this was previously untested — `PROXY_SYSTEM_PROMPT` injection had zero test coverage for the OpenAI passthrough path; covered by new `test.js` cases in `describe('PROXY_SYSTEM_PROMPT injection')` (nested under `describe('handleOpenAIChat')`) asserting prepending onto a string system message, full preservation of array-form system content (the regression case), and insertion of a new system message when none exists
- Fixed a `POST /v1/messages/batches/{id}/cancel` race that let a canceled batch item still run against Ollama — `processBatch()` only checked `batch.cancelRequested` once per item, *before* calling `await acquireLlmSlotForBatch()`; that function has no reference to the batch and cannot observe a cancellation that arrives while the item is parked waiting for a `PROXY_MAX_CONCURRENCY` slot to free up, so once a slot finally opened the item ran straight through to Ollama and was recorded as `succeeded`/`errored` instead of the documented `{type:"canceled"}` — wasting a full inference cycle on exactly the capacity-constrained deployments `PROXY_MAX_CONCURRENCY` exists to protect. `processBatch()` now re-checks both `batch.cancelRequested` and the `expires_at` TTL immediately after the slot is granted, releasing the slot and recording `canceled`/`expired` without ever contacting Ollama if either condition is now true; covered by a new `test.js` case in `describe('processBatch — cancellation race while parked in concurrency queue')` that loads a fresh proxy module with `PROXY_MAX_CONCURRENCY=1`, occupies the only slot, parks a batch item in the queue, sets `cancelRequested` while it's still waiting, then frees the slot and asserts `global.fetch` was never called and the item is recorded as `canceled` (confirmed this test fails against the unpatched code, proving it exercises the real race)
- Fixed unbounded write-buffer growth in `GET /v1/messages/batches/{id}/results` — `handleGetBatchResults()` wrote every result line (up to `MAX_BATCH_REQUESTS`, 100,000, per batch) in a tight synchronous loop with no regard for `res.write()`'s return value; unlike every other streaming handler in this file, nothing paces these writes against a slow upstream fetch — the data is already fully in memory — so a slow client (or several concurrent slow clients reading a large batch's results) could force Node to buffer the entire multi-megabyte response in the socket's internal write queue before it ever drains, risking memory exhaustion on a deployment processing large batches. The loop now checks `res.write()`'s return value and, when it reports a full buffer (`=== false`, matching Node's real `http.ServerResponse` contract — checked strictly rather than via plain falsy-ness so test stubs whose `write()` returns `undefined` are unaffected), awaits a `'drain'` event before continuing; it also stops writing immediately if the client disconnects mid-stream (`res.writableEnded`, or a `'close'` event while paused on drain) instead of continuing to buffer data nobody will read. `handleGetBatchResults` exported for unit testing; covered by new `test.js` cases in `describe('Batch ownership / per-API-key isolation')` using a real `EventEmitter`-backed mock response: one asserts the write loop pauses after a `write()` returning `false` and resumes only once `'drain'` fires, delivering all lines in order; the other asserts it stops writing further lines once the client disconnects mid-pause
- `RATE_LIMIT_RPM`/`RATE_LIMIT_PER_IP_RPM`/`RATE_LIMIT_PER_KEY_RPM` now enforced on `GET /v1/models` and `GET /v1/models/:id` — both handlers make a real network call to Ollama (`handleModels`/`handleModelById` fetch `/api/tags`, and `handleModelById` additionally fetches `/api/show`), yet the router previously called only `checkAuth()` for these two routes while every other model-management route that touches Ollama (`DELETE /v1/models/:id`, `POST /v1/models/pull`) already had all three `checkRateLimit()` calls; a caller could fully dodge an operator's configured request-rate limits — meant to stop one caller from hogging the shared Ollama/GPU host — by polling `GET /v1/models` in a tight loop instead of a rate-limited route, and in multi-host (`OLLAMA_HOST`) deployments each call also feeds into the round-robin/health-check machinery. The router now applies the same three `checkRateLimit()` calls used by every sibling model-management route to both `GET /v1/models` and `GET /v1/models/:id`, after `checkAuth()` and before the corresponding handler (`GET /health` is intentionally left unchanged since it's typically polled by load balancers/Docker healthchecks rather than end users); covered by new `test.js` cases in `describe('rate limiting on GET /v1/models and GET /v1/models/:id')` asserting each of the three limits returns `429 rate_limit_error` on a second call to either route, and that both routes still succeed normally when no rate limit env vars are set
- Fixed missing `MODEL_MAP` alias resolution in `DELETE /v1/models/:id` and `POST /v1/models/pull` — `GET /v1/models`, `GET /v1/models/:id`, and every inference endpoint resolve a Claude-style alias (e.g. `claude-3-haiku`) to its real Ollama model name before doing anything else with it, but `handleDeleteModel`/`handlePullModel` used the raw alias string directly: `checkModelAccess()` checked the alias against `PROXY_API_KEY_MODELS`, which is documented and enforced everywhere else against the *real* model name, so an allow-list entry like `family:llama3.2:1b` never matched a request addressing that same model by alias even though it should have been allowed; and the outgoing `/api/delete`/`/api/pull` call to Ollama was made with the alias itself, which Ollama doesn't recognize — `DELETE /v1/models/claude-3-haiku` 404'd instead of deleting the mapped model, and a pull request tried to fetch a registry image literally named `claude-3-haiku`. Both handlers now call `resolveModel()` first (mirroring `handleModelById`) and use the resolved real model name for both the access check and the Ollama request body, while the JSON response still echoes the originally-requested alias as `id`, matching `handleModelById`'s existing convention; covered by new `test.js` cases in `describe('handleDeleteModel / handlePullModel — model access control')` asserting an alias that resolves to an allowed model succeeds and reaches Ollama with the real model name, and an alias that resolves to a disallowed model is rejected with 403 before Ollama is ever called
- Fixed missing validation of the `tool_choice` request field — `toOpenAIToolChoice()` reads `tc.name` unguarded for `type:"tool"`, so a request like `tool_choice:{type:"tool"}` (missing `name`) or `tool_choice:{type:"tool",name:123}` (non-string `name`) silently produced a malformed OpenAI tool_choice (`{type:"function",function:{name:undefined}}`) that was forwarded straight to Ollama, unlike every other request field (`tools`, `system`, `messages`, `max_tokens`, `model`) which already has a dedicated `validate*()` function returning a clean 400 `invalid_request_error`; in `POST /v1/messages/batches` this was worse — a malformed `tool_choice` passed batch-creation validation entirely and only surfaced as an opaque `errored` result after background processing ran, with no feedback at creation time, unlike every other batch-item field. Added `validateToolChoice()` (returns `{}` for absent/null/`auto`/`none`/`any`/well-formed `tool`, `{error}` for a non-object value, a missing string `type`, or `type:"tool"` without a non-empty string `name`) and wired it into `handleMessages` (after `validateSystemField`, before the streaming/model-resolution logic), the `POST /v1/messages/batches` per-item creation-time validation loop (after `batchSystemResult`), and `processBatchRequest()` itself (defensive re-check, mirroring the existing `maxTokensResult` re-check, so batches resumed from disk via `PROXY_BATCH_PERSIST_PATH` that predate this validation are still caught); `validateToolChoice` exported for unit testing; covered by new `test.js` cases in `describe('validateToolChoice')` asserting acceptance of `auto`/`none`/`any`/well-formed `tool` choices and rejection of a non-object value, a missing/non-string `type`, and `type:"tool"` with a missing/empty/non-string `name`
